# no_reply 轮次被误报为 MODEL_EMPTY_REPLY（260918）用户验证

## 验证说明

- 验证对象：模型在 IM 发起的轮次中显式调用 `no_reply` 静默结束后，IM 内不再收到「MODEL_EMPTY_REPLY」失败通知，且不残留任何可见消息。
- 环境/前置条件：重启 dsh 使 `@johnnren/dsh-im-humanize` 新构建生效（link 部署，需重启）；已绑定 dsh-im 私聊。

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 在 IM 发一条会触发静默处理的消息（模型判断无需回复并调用 no_reply） | IM 内无任何新消息、无失败通知（不再出现 MODEL_EMPTY_REPLY） | | 待验证 | 此前会收到「模型没有返回可显示的内容…错误码：MODEL_EMPTY_REPLY；参考号：MF-…」 |
| 同一轮模型在 no_reply 前有自言自语/工具调用的，观察 IM | 依旧静默，叙述文本与工具进度不作为回复发出 | | 待验证 | 静默胜过叙述文本，与 reclaim 先例一致 |
| 在 IM 发一条需要正常回复的消息 | 照常收到文本回复，流式/卡片行为与之前一致 | | 待验证 | 回归确认 |
| 模型调用 no_reply 但同轮 `dsh_im_return_file` 了文件 | 文件照常收到，无失败通知、无多余文本 | | 待验证 | artifacts 投递不受静默影响 |
| 飞书渠道触发一次静默轮（如已启用） | 流式占位卡被撤回，聊天内不留「正在生成…」/「…」卡片 | | 待验证 | 原生流式路径新增 abort 撤回；未启用飞书流式可跳过 |
| 对正在静默处理的轮次发 `/stop` | 显示「已中断」类停止反馈（不因 no_reply 变成完全静默） | | 待验证 | stop 优先级高于 no_reply |

## 验证结论

待验证。

## 待跟进

无。
